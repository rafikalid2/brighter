/**
 * check if an element is in view port
 * add two events:
 * 		viewport-in		: when the element got in viewport
 * 		viewport-out	: when the element out from viewport
 * 		isInViewport	: if the element is in the viewport
 * 		all.isInViewport: if all elements are in the view port
 *
 *
 * 		window.isVisible	: true when the active window is focused
 */


$$.get()

$$.post()